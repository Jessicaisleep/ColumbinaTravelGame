# 项目工作约定

远程仓库：`https://github.com/nameyygu/ColumbinaTravelGame.git`

每次完成用户要求的代码、资源或文档改动后，都要执行以下收尾流程：

1. 运行与本次改动相适应的检查或测试。
2. 检查 `git status` 和待提交差异，确认没有密钥、个人凭据、`node_modules`、`dist` 或其他不应上传的文件。
3. 将本次有意的改动创建为一个描述清楚的 Git 提交；不要擅自覆盖或丢弃用户已有改动。
4. 将当前分支推送到 `origin`。
5. 在最终回复中说明测试、提交哈希、分支和推送结果。若因认证、网络、冲突或测试失败而无法完成，明确说明阻塞原因和所需操作。
